ANTHROPIC_API_KEY=
QWEN_API_KEY=
# "Ask AI" brew suggestions. Text-only reasoning, so this does NOT need an
# omni model -- kept separate from QWEN_OMNI_MODEL so the two can move
# independently.
QWEN_CHAT_MODEL=qwen3.6-plus
# Must be an omni/vision-capable model -- used for both label scanning and
# voice sessions.
QWEN_OMNI_MODEL=qwen3.5-omni-flash
